export interface  sendNewPrompt {
    apiKey: string;
    name: string;
    description: string;
    prompts: string;
  }

  export interface myPromptApikey{
    apiKey:string;
  }
  
  export interface sendPromptId{
    apiKey:string;
    promptsId:string;
  }
